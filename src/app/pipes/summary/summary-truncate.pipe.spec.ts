import { SummaryTruncatePipe } from './summary-truncate.pipe';

describe('SummaryTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
